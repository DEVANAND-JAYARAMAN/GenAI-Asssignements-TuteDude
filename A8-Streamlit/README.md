## Streamlit A8
## Files are:  app_basic.py, app_discount.py, app_product_form.py, app_dashboard.py
## Task 1
- Streamlit title,Text input, Button, Displaying output using `st.write()`

## Task 2
- Number input, Slider, Button, Displaying result using `st.success()`
## Task 3
- Sidebar components, Text input, Selectbox, Number input, Success message
## Task 4
- Selectbox, Dictionary lookup, Metric display, Bar chart

## How to Run
1. Install the Streamlit using theis command:
pip install streamlit

2. open the folder and run using this command

python -m streamlit run file_name.py

## to check the versions: 
python --version
python -m pip --version
python -m streamlit --version