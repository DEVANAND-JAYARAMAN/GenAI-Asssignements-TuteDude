#this contains the task1 to task7.
#commands to run:
#go the respective directory
#use these commands to execute the program: 
#python file_name.py

#cover the topics of encapsulation, polymorphism, classes and objects, constructors('__init__')
#inheritance, abstraction and super keywords